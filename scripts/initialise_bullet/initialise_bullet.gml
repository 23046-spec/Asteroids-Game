///@description initialise_bullet

function initialise_bullet(_dir, _spd, _fac, _creator, inst){
		with(inst){
					direction = _dir;
					speed = _spd;
					faction = _fac;
					creator = _creator;
		
		
	}
}